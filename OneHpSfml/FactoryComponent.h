#pragma once
#include <string>
#include <map>
#include "Component.h"

/*template <class T, class Key = Component> class FactoryComponent
{
	static std::map<Key, T*> mapObjects;

public:

	static void Register(Key key, T* obj);
	T* Create(const Key& key);
};*/



